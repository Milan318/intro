#include <stdio.h>
int main() 
{
    int length,width;

    printf("enter the length of the ractangle");
    scanf("%d" ,&length);

    printf("enter the width of the ractangle");
    scanf("%d" ,&width);

    

    printf("the area of the ractangle %d", length*width);

    return 0;

}