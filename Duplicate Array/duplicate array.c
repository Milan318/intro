#include  <stdio.h>
int main(){
int arr[]={1,2,5,6,6};
  int n= sizeof(arr)/size of (arr[0]);
  for (int i=0;i<=4;i++){
  for (int j=i+1;j<=4;j++){
    if (arr[i]==arr[j])
   printf("duplicate element is %d",arr[i]);   
  }
  }
}
